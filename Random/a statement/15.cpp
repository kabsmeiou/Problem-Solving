#include <bits/stdc++.h>
#define int long long
using namespace std;
signed main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
        int n;
        cin >> n;
        string ans = string(n, 'o');
        ans = "L" + ans + "ng";
        cout << ans << '\n';
}